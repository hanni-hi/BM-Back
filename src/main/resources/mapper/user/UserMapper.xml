<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.study.baemin.api.domain.user.repository.UserMapper">

    <select id="getAllUser" resultType="user">
        SELECT *
        FROM tb_member

    </select>

    <select id="getUserTarget" resultType="user" parameterType="String">
        SELECT *
        FROM tb_member
        WHERE mem_id=#{_id}

    </select>

    <select id="getUserByName" resultType="user" parameterType="String">
        SELECT *
        FROM tb_member
        WHERE name like '%'+#{_name}+'%'

    </select>

    <insert id="createUser" parameterType="UserCreateRequestDto">
        INSERT tb_member (MEM_ID, NAME, EMAIL, GRADE, CREATED_AT)
        values (#{id},#{name},#{email}, #{grade} ,now())

    </insert>

</mapper>